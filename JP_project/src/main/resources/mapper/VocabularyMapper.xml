<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jp_project.mapper.VocabularyMapper">

    <select id="selectAll" resultType="Vocabulary">
        SELECT
            id,
            kana,
            kanji,
            romaji,
            meaning,
            english,
            example,
            important,
            note,
            created_at,
            updated_at
        FROM vocabulary
        ORDER BY created_at DESC
    </select>

    <insert id="insert" parameterType="Vocabulary" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO vocabulary (
            kana,
            kanji,
            romaji,
            meaning,
            english,
            example,
            important,
            note,
            created_at,
            updated_at
        ) VALUES (
            #{kana},
            #{kanji},
            #{romaji},
            #{meaning},
            #{english},
            #{example},
            #{important},
            #{note},
            #{createdAt},
            #{updatedAt}
        )
    </insert>

    <select id="selectById" resultType="Vocabulary">
        SELECT
            id,
            kana,
            kanji,
            romaji,
            meaning,
            english,
            example,
            important,
            note,
            created_at,
            updated_at
        FROM vocabulary
        WHERE id = #{id}
    </select>


    <update id="update" parameterType="Vocabulary">
        UPDATE vocabulary
        SET
        kana = #{kana},
        kanji = #{kanji},
        romaji = #{romaji},
        meaning = #{meaning},
        english = #{english},
        example = #{example},
        important = #{important},
        note = #{note},
        updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM vocabulary
        WHERE id = #{id}
    </delete>

    <select id="selectByKana" resultType="Vocabulary">
        SELECT id, kana, kanji, romaji, meaning, english, example, important, note, created_at, updated_at
        FROM vocabulary
        WHERE kana = #{kana}
        LIMIT 1
    </select>

    <select id="selectByKanji" resultType="Vocabulary">
        SELECT id, kana, kanji, romaji, meaning, english, example, important, note, created_at, updated_at
        FROM vocabulary
        WHERE kanji = #{kanji}
        LIMIT 1
    </select>

    <select id="search" resultType="Vocabulary">
        SELECT id, kana, kanji, romaji, meaning, english, example, important, note, created_at, updated_at
        FROM vocabulary
        WHERE kana LIKE CONCAT('%', #{keyword}, '%')
        OR kanji LIKE CONCAT('%', #{keyword}, '%')
        OR meaning LIKE CONCAT('%', #{keyword}, '%')
        OR english LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY created_at DESC
    </select>

    <!-- 分页/搜索共用的 where 条件 -->
    <sql id="pageWhere">
        <if test="keyword != null and keyword != ''">
            WHERE kana LIKE CONCAT('%', #{keyword}, '%')
            OR kanji LIKE CONCAT('%', #{keyword}, '%')
            OR meaning LIKE CONCAT('%', #{keyword}, '%')
            OR english LIKE CONCAT('%', #{keyword}, '%')
        </if>
    </sql>

    <select id="selectPage" resultType="Vocabulary">
        SELECT id, kana, kanji, romaji, meaning, english, example, important, note, created_at, updated_at
        FROM vocabulary
        <include refid="pageWhere"/>
        ORDER BY created_at DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*)
        FROM vocabulary
        <include refid="pageWhere"/>
    </select>

</mapper>
